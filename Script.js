// Get elements
const tempChart = document.getElementById('temp-chart').getContext('2d');
const precipChart = document.getElementById('precip-chart').getContext('2d');
const seaLevelChart = document.getElementById('sea-level-chart').getContext('2d');

// Sample data
const tempData = {
  labels: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'],
  datasets: [{
    label
